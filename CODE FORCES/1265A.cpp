#include<iostream>
#include<string.h>
using namespace std;
int arr[5][5]=1;
int main()
{
    int len,n;
    cin>>n;
    while(n--)
    {
    string s;
    getline(cin,s);
    len=strlen(s);
    for(int i=0;i<len;i++)
    {
        if(s[i]=='?')
        {
            if(i==0 && (s[i+1]='a' || s[i+1]=='')

        }

    }
    ]

}
